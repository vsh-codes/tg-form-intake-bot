services:
  bot:
    build: .
    container_name: tg-form-intake-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./credentials:/app/credentials:ro
    # No exposed ports — long-polling only
